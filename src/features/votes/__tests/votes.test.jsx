import 'jest-dom/extend-expect';
import { cleanup } from 'react-testing-library';

afterEach(cleanup);

it('lets me edit an existing vite list', () => {});

it('add an option when creating a vote', () => {});
it('lets me create a vote', () => {});

it('lets me record a vote', () => {});
it('people can change their vote before deadline', () => {});
it('total votes show', () => {});
it('delete a vote', () => {});
